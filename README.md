# storefront-web

Dummy audit fixture for a React Query cache coherence audit.

This repository contains fake storefront code only. It is intentionally small and includes planted React Query cache key, endpoint, staleTime, refetch, and invalidation patterns for an audit exercise. Do not use any values here as real user data or credentials.

The audit itself has not been run and no findings are recorded in this codebase.
